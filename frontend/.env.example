# URL do serviço de autenticação externo
# Em dev: deixar vazio para usar o token diretamente como senha no login
# Em produção: https://auth.seudominio.com
VITE_AUTH_URL=http://localhost:3010

# URL do backend CapacidadeProdutiva (usada quando não há proxy Vite)
# Em dev com vite proxy, não é necessário
VITE_API_URL=http://localhost:3050
